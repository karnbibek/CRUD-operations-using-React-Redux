// import createHistory from 'history/createBrowserHistory';
var createHistory = require('history').createBrowserHistory; // the history package was installed automatically while installing react-router-dom.
export default createHistory();